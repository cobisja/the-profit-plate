<div class="col-xxl-6 mb-5 mb-xl-0">
    <!--begin::List widget 8-->
    <div class="card card-flush h-lg-100">
        <!--begin::Header-->
        <div class="card-header pt-7 mb-5">
            <!--begin::Title-->
            <h3 class="card-title align-items-start flex-column">
                <span class="card-label fw-bold text-gray-800">Latest Products</span>
                {% if products|length %}
                    <span class="text-gray-500 mt-1 fw-semibold fs-6">{{ products[0].updatedAt|ago }}</span>
                {% endif %}
            </h3>
            <!--end::Title-->
            <!--begin::Toolbar-->
            <div class="card-toolbar">
                {% if products|length %}
                    <a href="{{ path('app_admin_products_index') }}" class="btn btn-sm btn-light">View All</a>
                {% endif %}
            </div>
            <!--end::Toolbar-->
        </div>
        <!--end::Header-->
        <!--begin::Body-->
        <div class="card-body pt-0">
            <!--begin::Items-->
            <div class="m-0">
                {% set bullet_colors = ['warning', 'secondary', 'info', 'danger', 'success', 'primary'] %}
                {% for product in products %}
                    {% set bullet_color = bullet_colors[loop.index0 % 6] %}
                    <!--begin::Item-->
                    <div class="d-flex flex-stack">
                        <span class="bullet bullet-vertical d-flex align-items-center min-h-50px mh-100 me-4 bg-{{ bullet_color }}"
                        ></span>
                        {% set url = asset(
                            product.picture
                            ? uploads_folder ~ '/products/' ~ product.picture
                            : 'templates/media/files/blank-image.svg'
                        ) %}
                        <a href="#" data-action="admin--shared--image-zoom#zoomImage">
                            <img src="{{ url }}"
                                 class="me-4 w-25px zoom-image"
                                 style="border-radius: 4px"
                                 alt="{{ product.name }}"
                            >
                        </a>
                        <!--begin::Section-->
                        <div class="d-flex flex-stack flex-row-fluid d-grid gap-2">
                            <!--begin::Content-->
                            <div class="me-5">
                                <!--begin::Title-->
                                <a href="{{ path('app_admin_products_show', { id: product.id }) }}"
                                   class="text-gray-800 fw-bold text-hover-primary fs-6"
                                >{{ product.name|capitalize }}</a>
                                <!--end::Title-->
                                <!--begin::Desc-->
                                <span class="text-gray-500 fw-semibold fs-7 d-block text-start ps-0"
                                >{{ product.productType.name|capitalize }} - updated {{ product.updatedAt|ago }}</span>
                                <!--end::Desc-->
                            </div>
                            <!--end::Content-->
                            <!--begin::Info-->
                            <div class="d-flex align-items-center">
                                <!--begin::Number-->
                                <span class="text-gray-800 fw-bold fs-6 me-3 d-block">
                                    {{ product.pricePerUnit|number_format(2)|format_currency('USD') }}
                                </span>
                                <!--end::Number-->
                            </div>
                            <!--end::Info-->
                        </div>
                        <!--end::Section-->
                    </div>
                    <!--end::Item-->
                    <!--begin::Separator-->
                    <div class="separator separator-dashed my-3"></div>
                    <!--end::Separator-->
                {% endfor %}
            </div>
            <!--end::Items-->
        </div>
        <!--end::Body-->
    </div>
    <!--end::LIst widget 8-->
</div>